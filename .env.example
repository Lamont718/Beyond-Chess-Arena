# --- Local dev (SQLite) ---
DATABASE_URL="file:./dev.db"

# --- Production (Neon Postgres) — set in Vercel, and switch schema.prisma provider to "postgresql" ---
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Secret used to sign login session cookies. Generate a long random string.
SESSION_SECRET="change-me-to-a-long-random-string"
